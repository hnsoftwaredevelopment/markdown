Packages

pip install pymusicxml		A simple python library for exporting MusicXML
pip install musicxml_parser	A musicXML parser written in Python based on 
				a SAX analyzer
pip install MusicalMusic -U	Retrieve Sheet music from Musescore!
pip install msdiff		Unknown	
pip install mscxyz		Manipulate the XML based mscx files of
				the notation software MuseScore.